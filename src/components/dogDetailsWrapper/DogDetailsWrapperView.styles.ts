import { makeStyles } from '@material-ui/core/styles';

export const dogDetailsWrapperStyle = makeStyles({
  container: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-around',
  },
});
